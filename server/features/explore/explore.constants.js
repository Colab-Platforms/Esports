const CONTENT_TYPES = ['video', 'article', 'social', 'resource'];

const PUBLIC_FIELDS = 'title description url thumbnailUrl contentType platform isFeatured displayOrder publishedAt';

module.exports = {
  CONTENT_TYPES,
  PUBLIC_FIELDS
};
