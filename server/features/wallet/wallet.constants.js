const DEFAULT_DAILY_LOGIN_REWARD = 10;
const STREAK_BONUS_COINS = 30;
const STREAK_MILESTONE_DAYS = 7;

module.exports = {
  DEFAULT_DAILY_LOGIN_REWARD,
  STREAK_BONUS_COINS,
  STREAK_MILESTONE_DAYS
};
