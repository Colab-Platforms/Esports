# Nixpacks Configuration for Render
# This tells Render how to build and deploy

[phases.setup]
nixPkgs = ["nodejs-22_x"]

[phases.install]
cmds = ["npm install --production=false", "cd client && npm install"]

[phases.build]
cmds = ["cd client && npm run build"]

[start]
cmd = "npm start"
